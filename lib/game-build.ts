export const getLoaderUrl = (folder: string) => `/build/${folder}/Build/${folder}.loader.js`;
export const getDataUrl = (folder: string) => `/build/${folder}/Build/${folder}.data`;
export const getFrameworkUrl = (folder: string) => `/build/${folder}/Build/${folder}.framework.js`;
export const getCodeUrl = (folder: string) => `/build/${folder}/Build/${folder}.wasm`;
